const API_URL="PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE";
const $=id=>document.getElementById(id);
function showResult(el,html,error=false){el.innerHTML=html;el.classList.remove("hidden");el.classList.toggle("error",error)}
async function callApi(action,payload){if(API_URL.includes("PASTE_YOUR"))throw new Error("Set API_URL in app.js to your Google Apps Script Web App URL.");const r=await fetch(API_URL,{method:"POST",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify({action,...payload})});const d=await r.json();if(!d.ok)throw new Error(d.error||"Request failed.");return d}
function fileToBase64(f){return new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result.split(",")[1]);r.onerror=rej;r.readAsDataURL(f)})}
function esc(s){return String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}
$("questionForm").addEventListener("submit",async e=>{e.preventDefault();const b=$("submitBtn"),out=$("submitResult");b.disabled=true;showResult(out,"Submitting...");try{const files=[];for(const f of $("files").files){if(f.size>10*1024*1024)throw new Error(`${f.name} is larger than 10 MB.`);files.push({name:f.name,mimeType:f.type||"application/octet-stream",data:await fileToBase64(f)})}const d=await callApi("submit",{studentName:$("studentName").value.trim(),studentId:$("studentId").value.trim(),studentEmail:$("studentEmail").value.trim(),course:$("course").value.trim(),question:$("question").value.trim(),files});showResult(out,`<strong>Submitted successfully.</strong><br>Your Submission ID is <strong>${esc(d.submissionId)}</strong>.<br>Save this ID to check feedback.`);e.target.reset()}catch(err){showResult(out,esc(err.message),true)}finally{b.disabled=false}});
$("feedbackForm").addEventListener("submit",async e=>{e.preventDefault();const out=$("feedbackResult");showResult(out,"Checking...");try{const d=await callApi("feedback",{submissionId:$("feedbackId").value.trim(),studentEmail:$("feedbackEmail").value.trim()});let h=`<strong>Status:</strong> ${esc(d.status)}<br><strong>Question:</strong><br>${esc(d.question).replace(/\n/g,"<br>")}`;if(d.feedback)h+=`<hr><strong>Instructor feedback:</strong><br>${esc(d.feedback).replace(/\n/g,"<br>")}`;if(d.feedbackFileUrl&&/^https:\/\/drive\.google\.com\//i.test(d.feedbackFileUrl))h+=`<br><br><a href="${d.feedbackFileUrl}" target="_blank" rel="noopener">Open feedback file</a>`;showResult(out,h)}catch(err){showResult(out,esc(err.message),true)}});
